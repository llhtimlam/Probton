# ----- ------ ----- ----- ------ ----- ----- ------ -----
# Chipathon 2026: Project configuration
# LICENSE: Apache License Version 2.0
# http://www.apache.org/licenses/
# ----- ------ ----- ----- ------ ----- ----- ------ -----

# data will be pulled from main branch of the repo.
# please be sure that the main branch contains the data you want to submit.
# This file will be updated with more information later

# ======================================================
# Project metadata (used for documentation / review)
# ======================================================
project:
  title: "Auto Aligner"             # Project title (e.g. "Simple CPU", "PLL", "ADC")
  team: "B18 - Probton"                      # Your Team name
  discord: "timlam0531"                   # Optional: Discord username (for support / communication)
  description: "Dithering Auto Aligner Processor"  # Short one-line description
  lvs_config: "lvs/lvs_config.json"

# Bottom left to Upper left to Upper right (1/4 of 2.235μm x 2.235μm)
pins:
  # ===== Digital =====
  # === West ===
  # 1
  - name: VSS
    io_type: ground

  # 2
  - name: rst_n
    io_type: input_schmitt

  # 3
  - name: spi_cs_n
    io_type: input_schmitt

  # 4
  - name: spi_sclk
    io_type: input_cmos

  # 5
  - name: spi_mosi
    io_type: input_schmitt

  # 6
  - name: spi_miso
    io_type: bidirectional

  # 7
  - name: clk
    io_type: input_cmos

  # 8
  - name: move_en_x
    io_type: bidirectional

  # 9
  - name: dir_x
    io_type: bidirectional

  # 10
  - name: ref_wave_x
    io_type: bidirectional

  # 11
  - name: mems_drv_x
    io_type: bidirectional

  # === North ===
  # 12 (1)
  - name: mems_drv_y
    io_type: bidirectional

  # 13 (2)
  - name: ref_wave_y
    io_type: bidirectional

  # 14 (3)
  - name: dir_y
    io_type: bidirectional

  # 15 (4)
  - name: move_en_y
    io_type: bidirectional

  # 16 (5)
  # Virtual Pin


  # ===== Digital/Analog Interface =====

  # 17 (6)
  - name: comp_x
    io_type: input_cmos

  # 18 (7)
  # Virtual Pin

  # 19 (8)
  - name: read_en
    io_type: bidirectional

  # 20 (9)
  # Virtual Pin

  # 21 (10)
  - name: comp_y
    io_type: input_cmos
  
  # 22
  - name: VDD
    io_type: power